//This function is used to count the number of characters in the string excluding the null (‘\0’) characters.
#include <stdio.h>
#include <string.h>

int main(){
        char length[100] = "Harsh";
        

        printf("%d", strlen(length));
    return 0;
}